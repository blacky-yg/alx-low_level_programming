#include "main.h"

/**
 * _isalpha - check if a char is an alpha_num
 * @c: number
 *
 * Description: check if a char is an alpha_num
 * Return: 0 or 1
 */
int _isalpha(int __attribute__((unused)) c)
{
	return (0);
}
