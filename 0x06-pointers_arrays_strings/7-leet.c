#include "main.h"

/**
 * leet - Encodes a string into 1337
 * @str: initial string
 *
 * Description: Encode a string into 1337
 * Return: the encoded string
 */
char *leet(char *str)
{
	int i;

	for (i = 0; str[i]; i++)
	{
	}
	return (str);
}
